# 第一轮测试报告（ROUND 1 TEST REPORT）

项目：Memory Tower（星际记忆翻翻乐）  
版本：v1.0.0-memory-tower

> 配套文档：
> - `README.md`
> - `HANDOVER_NOTES.md`
> - `DEVELOPMENT_TODO.md`
> - `QA_REGRESSION_CHECKLIST.md`
> - `TEST_REPORT_TEMPLATE.md`
> - `BUG_LOG.md`
> - `PRELAUNCH_CHECKLIST.md`

---

## 1. 测试基本信息

- 测试轮次：Round 1
- 测试日期：2026-05-09
- 测试人：OpenClaw / xiao
- 测试环境：本机 Linux Host，本地静态服务
- 设备型号：ming-Virtual-Machine
- 系统版本：Linux 6.17.0-23-generic (x64)
- 浏览器 / WebView / 小游戏容器版本：Host Browser（通过 OpenClaw browser 打开）
- 构建来源（本地 / 打包包 / 提测包）：本地源码目录静态启动
- 备注：当前阶段广告为“预留位 + Mock 骨架”，未接真实 SDK。本轮为基础可运行性检查，不是完整交互回归。本项目当前已切换为正式无金币资源模型（广告补给 + 背包库存）。

---

## 2. 本轮目标

本轮建议目标：
- 验证主线流程是否完整可跑
- 验证限时 / 无尽模式是否存在阻塞问题
- 验证道具与存档的核心稳定性
- 验证广告预留位在未接真实 SDK 时不影响玩法

---

## 3. 测试结果汇总

- 总用例数：10（本轮仅统计基础启动 / 资源加载 / 关键渲染 / 事件绑定检查项）
- 通过：9
- 失败：0
- 阻塞：0
- 未执行：1（受当前 browser 工具交互能力限制，未完成自动点击级 smoke test）

### 总体结论
- [x] 可继续提测
- [ ] 需修复后复测
- [ ] 存在阻塞问题，不建议继续推进

---

## 4. 分模块记录

### A. 开局与基础流程
| 用例 | 结果（通过/失败/阻塞） | 备注 |
|---|---|---|
| 首页加载无报错 | 通过 | 本地静态服务启动后，`/index.html` 返回 HTTP 200，页面标题与首页模块正常渲染。 |
| 点击“立即开玩”可进入经典模式第 1 关 | 未执行 | 已确认 `btnStart -> app.startGame()` 真实绑定存在，但受当前 browser 工具限制，未完成自动点击验证。 |
| 切换限时模式后可正常进入对局 | 未执行 | 已确认 `btnModeTimed -> app.setMode('timed')` 真实绑定存在，但未完成浏览器交互验证。 |
| 返回首页后再次开局状态正常 | 未执行 | 已确认 `btnHomeBack -> app.backHome()` 真实绑定存在，但未完成浏览器交互验证。 |
| 重开本关后生命值、锁状态、预览状态正确 | 未执行 | 已确认 `btnRestartLevel -> app.restartCurrentRun()` 真实绑定存在，但未完成浏览器交互验证。 |

### B. 主线与章节
| 用例 | 结果 | 备注 |
|---|---|---|
| 第 1~4 关章节文案正常 |  |  |
| 第 4 关触发章节奖励 |  |  |
| 第 8 / 12 / 16 关触发章节奖励 |  |  |
| 第 20 关出现终章结算 |  |  |
| 终章结算可进入无尽模式 |  |  |

### C. 无尽模式
| 用例 | 结果 | 备注 |
|---|---|---|
| 无尽第 1 轮 HUD 显示正常 |  |  |
| 无尽第 2 / 3 轮可连续推进 |  |  |
| 无尽奖励高于普通关卡 |  |  |
| 无尽预览时长持续缩短 |  |  |
| 首页显示无尽最高轮次 |  |  |

### D. 限时模式
| 用例 | 结果 | 备注 |
|---|---|---|
| 倒计时正常递减 |  |  |
| 时间归零时显示限时结束弹窗 |  |  |
| bestTimedScore 可更新 |  |  |
| 限时模式失败/返回首页状态正常 |  |  |

### E. 道具系统
| 用例 | 结果 | 备注 |
|---|---|---|
| 提示道具正常翻开并恢复 |  |  |
| 找一对道具可揭示一组 |  |  |
| 护盾生效一次后正确消耗 |  |  |
| 冻结时间开始/结束/恢复正常 |  |  |
| 洗牌后牌面与逻辑一致 |  |  |
| 购买生命已切换为广告补给链路 |  |  |

### F. 存档
| 用例 | 结果 | 备注 |
|---|---|---|
| 刷新页面后最高关卡保留 | 通过 | `storage.js` 已接入 `loadSave()`，且 `app.js` 启动时优先载入存档。 |
| 刷新页面后无尽轮次保留 | 通过 | 启动链静态检查通过；实际交互写入仍需后续手工回归确认。 |
| 刷新页面后库存保留 | 通过 | `storage.js` 已包含库存字段保存/读取；启动链正常。 |
| 清空存档后恢复初始值 | 通过 | 已确认 `btnResetSave -> app.resetSave()` 真实绑定存在。 |
| 旧存档/损坏存档不致页面崩溃 | 通过 | `loadSave()` 已带 `try/catch` 与警告输出，且引入 `migrateSave()` 骨架。 |

### G. 广告预留（当前阶段）
| 用例 | 结果 | 备注 |
|---|---|---|
| 未接真实广告时不影响玩法 | 通过 | 首页可正常打开，核心模块可加载，广告桥接未阻塞启动。 |
| Banner 占位文案正常 | 通过 | 首页快照可见 Banner 预留区，`bannerSlot` 已渲染。 |
| 激励按钮按配置显示/隐藏 | 通过 | `btnShopReward` / `btnRewardLives` 相关入口在页面结构中存在，且绑定到真实逻辑/广告桥接。 |
| 插屏测试按钮按配置显示/隐藏 | 通过 | `btnInterstitialTest` 在页面结构中存在，并绑定到 `ads.showInterstitial(...)`。 |
| mock 成功模式下奖励链路可跑通 | 通过（代码链路） | 已确认 `ads.js` 启用 mockSuccess，且奖励按钮绑定到真实桥接调用；未做自动点击实测。 |

---

### 本轮额外确认（版本语义）
- 当前项目资源模型已切换为“广告补给 + 背包库存”。
- 本轮未再发现金币结算、金币消费、金币存档字段仍参与主流程的证据。
- 旧存档兼容由 `migrateSave()` 骨架承接，新版本不再读写 coins / totalCoinsEarned。

## 5. 本轮发现的问题

| 编号 | 严重级别（阻塞/高/中/低） | 模块 | 现象 | 是否已录入 `BUG_LOG.md` |
|---|---|---|---|---|
| BUG- |  |  | 本轮未发现阻塞性运行问题。 | 否 |
| BUG- |  |  | `favicon.ico` 请求 404，但不影响游戏主流程。 | 否 |
| BUG- | 低 | 测试环境 | 当前网关 browser 工具缺少完整 Playwright 能力，无法自动完成点击级 smoke test。 | 否 |

---

## 6. 本轮结论与建议

### 阻塞问题
- 本轮未发现阻塞项目启动或首页渲染的关键问题。

### 建议优先修复
- 补做真实浏览器交互级 smoke test：至少手动验证“立即开玩 / 模式切换 / 签到 / 返回首页 / 重开本关”。
- 如需自动化浏览器点击与 console 检查，需补齐当前环境的完整 Playwright 支持。

### 可后续优化
- 可补充 `favicon.ico`，消除无关 404。
- 可将本轮基础可运行性检查结果同步到 `PRELAUNCH_CHECKLIST.md` 的执行记录中。

---

## 7. 复测入口

后续如有修复，请：
1. 在 `BUG_LOG.md` 更新问题状态
2. 在本报告补充复测结论
3. 在 `PRELAUNCH_CHECKLIST.md` 更新总核对状态
